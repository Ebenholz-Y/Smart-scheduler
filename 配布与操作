从零开始移植操作系统

✅ 第一步：安装基础依赖（环境文件夹）
1. 安装 Node.js（含 npm）

2. 安装 MySQL 数据库

✅ 第二步：创建数据库和用户（推荐非 root 用户）
打开 MySQL 命令行（Windows：开始菜单搜 “MySQL Command Line Client”；Linux：终端输入 mysql -u root -p）
sql
编辑
-- 创建新数据库CREATE DATABASE course_scheduler CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;-- 创建专用用户（更安全）CREATE USER 'scheduler_user'@'localhost' IDENTIFIED BY 'secure_password_123';-- 授权GRANT ALL PRIVILEGES ON course_scheduler.* TO 'scheduler_user'@'localhost';-- 刷新权限FLUSH PRIVILEGES;-- 退出EXIT;
📝 记下：
数据库名：course_scheduler
用户名：scheduler_user
密码：secure_password_123
主机：localhost
端口：3306

✅ 第三步：准备项目文件
将你的整个项目文件夹（包含以下文件）复制到新电脑：
text
编辑
your-project/├── login.html
├── timetable.html
├── server.js
├── auth.js
├── scheduler.js
├── importV3.js
├── config/
│   └── dbConfig.js   ← ⚠️ 这个需要修改！
└── 25-26（1）课程情况.xlsx
🔥 关键：修改 config/dbConfig.js
如果还没有 config/dbConfig.js，请创建它，内容如下：
javascript
编辑
// config/dbConfig.jsconst mysql = require('mysql2/promise');const config = {  host: 'localhost',  port: 3306,  user: 'scheduler_user',       // ← 改成你创建的用户名  password: 'secure_password_123', // ← 改成你的密码  database: 'course_scheduler', // ← 数据库名  charset: 'utf8mb4'};// 创建连接池（供 server.js 使用）const pool = mysql.createPool({  ...config,  connectionLimit: 10,  waitForConnections: true,  queueLimit: 0});module.exports = { config, pool };

✅ 第四步：安装 Node.js 依赖
在项目根目录打开终端（命令行），运行：
bash
编辑
npm init -y  # 如果没有 package.jsonnpm install express mysql2 xlsx
✅ 安装的包说明：
express：Web 服务器框架
mysql2：MySQL 数据库驱动（支持 Promise）
xlsx：读写 Excel 文件（用于 importV3.js）

✅ 第五步：初始化数据库表结构 & 导入课程数据(已给出示例数据：25-26（1）课程情况.xlsx）
方法一：使用 importV3.js（推荐）
此脚本会自动创建 courses、classrooms、course_schedule 表，并导入 Excel 数据。
确保 25-26（1）课程情况.xlsx 在项目根目录
运行导入脚本：
bash
编辑
node importV3.js
✅ 成功输出应包含：
text
编辑
✅ courses表创建成功✅ 课程数据导入完成！
💡 注意：教室分配将在排课阶段进行
⚠️ 如果报错“Excel文件不存在”，请检查文件名是否完全一致（包括括号是全角）。

方法二：手动建表（备用）
如果你不想用 importV3.js，可手动执行以下 SQL：
sql
编辑
USE course_scheduler;-- 教室表CREATE TABLE classrooms (  id VARCHAR(50) PRIMARY KEY,  type VARCHAR(50) NOT NULL DEFAULT '普通教室',  room_location VARCHAR(100),  capacity INT DEFAULT 50,  available_time_slots TEXT);-- 课程表CREATE TABLE courses (  id INT PRIMARY KEY AUTO_INCREMENT,  name VARCHAR(100) NOT NULL,  students INT NOT NULL,  teacher VARCHAR(50) NOT NULL,  teacher_id VARCHAR(20),  hours INT NOT NULL,  credits INT NOT NULL,  classroom_type VARCHAR(50),  period_type VARCHAR(20),  block_size TINYINT NOT NULL DEFAULT 2,  theory_hours INT DEFAULT 0,  lab_hours INT DEFAULT 0,  total_hours INT DEFAULT 0,  class_groups TEXT);-- 排课表CREATE TABLE course_schedule (  id INT PRIMARY KEY AUTO_INCREMENT,  course_id INT NOT NULL,  day TINYINT NOT NULL COMMENT '1=周一, ..., 5=周五',  period TINYINT NOT NULL COMMENT '1~11节',  location VARCHAR(50) NOT NULL,  time VARCHAR(100),  FOREIGN KEY (course_id) REFERENCES courses(id) ON DELETE CASCADE,  FOREIGN KEY (location) REFERENCES classrooms(id) ON DELETE CASCADE,  UNIQUE KEY unique_slot (location, day, period));
然后用其他方式导入课程数据（或仍用 importV3.js 只导入数据）。

✅ 第六步：启动后端服务
在项目根目录运行：
bash
编辑
node server.js
✅ 成功启动应看到：
text
编辑
✅ 排课系统后端服务已启动！本地地址: http://localhost:3000

✅ 第七步：访问系统
1. 打开登录页
浏览器访问：
text
编辑
http://localhost:3000/login.html
2. 登录账号（
管理员：A_admin / 123456
教师：T_张三 / 123456
学生：S_23物联网工程1班 / 123456
3. 管理员执行自动排课
登录后跳转到 timetable.html?user=A_admin
点击 “🚀 自动排课”（只有管理员可见）
系统将根据 scheduler.js 算法生成课表
4. 其他用户查看课表
学生/教师登录后自动进入自己的课表视图

✅ 附加建议
事项	建议
Excel 文件编码	确保 .xlsx 文件未被 WPS 损坏，可用 Microsoft Excel 打开验证
防火墙	若需局域网访问，关闭防火墙或开放 3000 端口
开机自启	Linux 可用 systemd，Windows 可用任务计划程序
备份	定期导出数据库：mysqldump -u scheduler_user -p course_scheduler > backup.sql

🎉 完成！
现在你的智能排课系统已在新电脑上完整运行！
所有功能（登录、课表查看、自动排课、Excel/PDF 导出）都应正常工作。
如有问题，可查看 server.js 启动日志中的错误信息，重点检查：
数据库连接参数
Excel 文件路径
端口是否被占用（如 3000）
祝你部署顺利！
